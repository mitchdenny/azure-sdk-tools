# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  branches:
  - master
  - users/mitchdenny/multi-pipelines
  paths:
  - /web/pixel-server

pool:
  vmImage: 'Ubuntu-16.04'

steps:
- script: echo Running pixel-server build!
